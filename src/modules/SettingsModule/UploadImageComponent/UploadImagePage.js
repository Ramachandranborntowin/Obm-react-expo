import React, { useState } from "react";
import CustomModelPrimary from "../../../animation/CustomModelPrimary";
const UploadImagePage = (props)=> {
  return (
    <CustomModelPrimary {...props} />
  )
  }
export default UploadImagePage
